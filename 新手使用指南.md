# 恢复文档智能命名工具：新手使用指南

> 适用系统：Windows 10 / Windows 11  
> 项目位置：`D:\DataRecovery`  
> 使用对象：不熟悉 Python、命令行或 AI API 的用户  
> 文档版本：1.2  
> 最后更新：2026-09-10

> 当前版本说明：方案和日志默认输出为 UTF-8 BOM 的 `.txt` 制表符表格；读取兼容 UTF-8、带 BOM 的 UTF-16、GB18030、Big5，支持制表符、逗号、分号以及旧的逗号分隔 `.txt`；DeepSeek 使用 `chat` 接口和 `deepseek-v4-flash`。

> 从 GitHub 在其他电脑测试时，项目目录不必固定为 `D:\DataRecovery`；下文出现该路径时，请替换为实际克隆位置。

---

## 1. 先记住最重要的三件事

1. **不要把唯一一份恢复资料交给这个程序处理。** 请先完整复制一份，只处理副本。
2. `plan` 命令只生成建议名称，**不会改文件名**。
3. 成功生成建议的行默认是 `approved`；运行 `apply` 前必须审核，并把不想执行或尚未确认的行改成 `pending`。

本项目已经为你准备了固定目录：

| 用途 | 固定路径 | 你应该放什么 |
|---|---|---|
| 少量测试 | `D:\DataRecovery\data\test` | 复制 5～20 个不重要的测试文件到这里 |
| 正式工作副本 | `D:\DataRecovery\data\work` | 测试通过后，放入完整的恢复资料副本 |
| 结果和日志 | `D:\DataRecovery\reports` | 本指南的命令会把 TXT 方案和改名日志放在这里 |
| 原始恢复资料 | **不要放进本项目** | 单独保存在只读磁盘、移动硬盘或另一个安全位置 |

如果你现在只想试一试，请把几份**复制出来的文件**放到：

```text
D:\DataRecovery\data\test
```

不要把测试文件发到 `doc_renamer`、`.venv` 或 `tests` 文件夹。

方案和日志也不要放进 `data\test` 或 `data\work`。因为 `.txt` 和 `.csv` 本身属于可扫描文件，放进输入目录可能导致下一次生成方案时把旧报告也当成待命名文档。

---

## 2. 这个项目到底做什么

恢复软件可能把原文件名变成：

```text
20240118_001.docx
20240118_002.pdf
20240118_003.xlsx
```

本工具会读取文档内部可以提取的内容，例如 Word 标题、Excel 工作表名称、PPT 首页内容、PDF 前几页文字，然后给出类似下面的建议：

```text
20240118_001.docx  →  XX项目验收报告.docx
20240118_002.pdf   →  2024年度采购合同.pdf
20240118_003.xlsx  →  客户付款明细表.xlsx
```

完整流程是：

```text
恢复资料的工作副本
        ↓
plan：读取内容并生成 TXT 方案（不改名）
        ↓
你打开 TXT 审核，把不想执行的行改成 pending
        ↓
apply：只修改 approved 的文件
        ↓
生成 rename_log.txt
        ↓
如果需要，可用 undo 按日志撤销
```

### 2.1 它能做什么

- 扫描指定目录及其所有下级文件夹。
- 计算每个文件的 SHA-256 哈希，用于识别完全重复的文件。
- 从支持的文档中提取少量文字和内部标题。
- 使用规则模式或模型 API 生成候选文件名。
- 把建议写入 TXT（制表符表格），交给你人工审核。
- 在真正改名前再次核对文件哈希。
- 遇到目标文件名已经存在时停止该项操作，不强行覆盖。
- 记录改名日志，并支持撤销。

### 2.2 它不能保证什么

- AI 只能根据内容**建议一个合理名称**，不能证明它就是以前的原始文件名。
- 当前版本不能直接识别扫描图片中的文字。
- 当前版本不能直接解析旧版 `.doc`、`.xls`、`.ppt` 二进制格式。
- 损坏、加密或无法读取的文件可能无法生成建议。
- 程序不能代替人工核对。合同、财务、人事等重要文件应逐份确认。

---

## 3. 项目文件结构

正常情况下，你会看到下面这些目录和文件：

```text
D:\DataRecovery
│
├─ .venv\                      Python 独立运行环境，不要编辑
├─ data\
│  ├─ test\                    少量测试文件副本放这里
│  └─ work\                    完整工作副本放这里
├─ reports\                    TXT 方案和改名日志放这里
├─ doc_renamer\                程序源代码，一般不用修改
│  ├─ __init__.py
│  ├─ __main__.py
│  ├─ cli.py                   命令入口
│  ├─ extractors.py            文档内容提取
│  ├─ providers.py             规则/API 模型接口
│  ├─ types.py                 程序内部数据结构
│  ├─ utils.py                 哈希、文件名清理和路径保护
│  └─ workflow.py              生成方案、执行改名、撤销
├─ tests\
│  └─ test_core.py             自动测试代码
├─ scripts\
│  └─ setup.ps1                新电脑一键安装和测试脚本
├─ .github\workflows\          GitHub 自动测试配置
├─ document_name_recovery.egg-info\  安装时自动生成，不要编辑
├─ .env.example                API 配置示例，不直接生效
├─ .env                        你之后创建的真实 API 配置
├─ .gitignore                  防止密钥和资料被误加入 Git
├─ pyproject.toml              Python 项目信息和依赖列表
├─ README.md                   简要说明
└─ 新手使用指南.md             你正在阅读的详细说明
```

### 3.1 你需要修改的文件

日常使用时，通常只需要修改两个地方：

| 文件 | 什么时候修改 | 修改什么 |
|---|---|---|
| `D:\DataRecovery\.env` | 使用在线模型 API 时 | API 类型、地址、密钥和模型 ID |
| `D:\DataRecovery\reports\某个方案.txt` | 审核候选名称时 | `proposed_name` 和 `status` |

### 3.2 你不需要修改的文件

以下内容不要随意编辑：

- `.venv` 中的任何文件；
- `doc_renamer` 中的 `.py` 文件；
- `pyproject.toml`；
- `tests` 中的测试代码；
- `document_name_recovery.egg-info` 和 `__pycache__` 等自动生成目录；
- `.env.example`，它只是模板，应复制成 `.env` 后再修改副本。

### 3.3 在另一台电脑首次安装

不要复制原电脑的 `.venv`。虚拟环境只负责隔离，不会自动携带第三方依赖；每台电脑都应重新创建并安装一次。

在 PowerShell 中运行：

```powershell
git clone https://github.com/jsonliukun/DataRecover.git
Set-Location ".\DataRecover"
powershell -ExecutionPolicy Bypass -File .\scripts\setup.ps1
```

脚本会创建 `.venv`，从 `pyproject.toml` 自动安装 `httpx`、`pypdf`、`python-dotenv` 和本项目，然后检查依赖并运行单元测试。安装包名称是 `python-dotenv`，但 Python 中的导入名称是 `dotenv`，两者并不矛盾。

如果不想运行脚本，可以手动执行：

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install --upgrade pip
.\.venv\Scripts\python.exe -m pip install .
.\.venv\Scripts\python.exe -m pip check
.\.venv\Scripts\python.exe -c "import httpx, dotenv, pypdf, doc_renamer; print('依赖安装正常')"
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

---

## 4. 第一次使用：完全不联网测试

这一节不需要 API 密钥，不会把文档内容发到互联网。它使用 `rules` 规则模式，主要用来确认文件放置、内容读取、TXT 输出和改名/撤销流程是否正常。

规则模式不是 AI：它通常使用文档内部标题或正文第一个有效语句作为候选名称，所以名称质量可能一般。这是正常现象。

### 第 1 步：准备测试文件

从恢复资料中复制 5～20 个文件，放入：

```text
D:\DataRecovery\data\test
```

第一轮建议选择：

- 2～5 个 `.docx`；
- 2～5 个 `.xlsx`；
- 2～5 个 `.pptx`；
- 2～5 个能够选中文字的 `.pdf`；
- 可选的 `.txt` 或 `.md`。

第一轮先不要选择：

- 唯一一份原件；
- 特别重要的合同或财务资料；
- 几百页的大 PDF；
- 纯扫描 PDF；
- 已知损坏或带密码的文件。

子文件夹可以保留。例如下面这种结构可以正常扫描：

```text
D:\DataRecovery\data\test
├─ 文件夹A\
│  ├─ 20240101_001.docx
│  └─ 20240101_002.xlsx
└─ 文件夹B\
   └─ 20240102_001.pdf
```

程序只会在原文件所在的文件夹内改名，不会把文件移动到其他文件夹。

生成方案、执行改名和撤销时必须使用同一个根目录。例如方案是针对 `data\test` 生成的，后续 `apply` 和 `undo` 也必须继续使用 `data\test`，不能改成 `data\work`。

### 第 2 步：打开 PowerShell

方法一：

1. 按键盘上的 `Win + R`；
2. 输入 `powershell`；
3. 按回车。

然后输入：

```powershell
Set-Location "D:\DataRecovery"
```

按回车。命令行左侧通常会显示：

```text
PS D:\DataRecovery>
```

后续命令都应在这个目录下执行。

### 第 3 步：确认程序可以启动

复制下面这条命令到 PowerShell：

```powershell
.\.venv\Scripts\doc-renamer.exe --help
```

正常时会看到 `plan`、`probe-api`、`apply` 和 `undo` 四个命令。

如果提示找不到文件，请先阅读本文的“常见问题”部分。

还可以运行一次完全离线的程序自检：

```powershell
.\.venv\Scripts\doc-renamer.exe probe-api --provider rules
```

正常时会看到“测试项目周报”等 JSON 内容。这里的 `rules` 不是 AI，也不会访问网络；它只是证明程序入口和基本命名逻辑可以运行。

### 第 4 步：生成不联网测试方案

运行：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\test" --provider rules --limit 20 --output ".\reports\01_rules_test_plan.txt"
```

这条命令各部分的意思：

| 命令片段 | 含义 |
|---|---|
| `doc-renamer.exe` | 启动程序 |
| `plan` | 只生成改名方案，不改文件 |
| `".\data\test"` | 扫描测试文件夹 |
| `--provider rules` | 使用不联网的规则模式 |
| `--limit 20` | 最多处理 20 个文件 |
| `--output ...txt` | 指定方案保存位置 |

程序会逐个显示：

```text
[1/10] 20240101_001.docx
[2/10] 20240101_002.xlsx
...
```

结束时会显示类似：

```json
{
  "total": 10,
  "planned": 8,
  "duplicates": 1,
  "errors": 1
}
```

它们分别表示：

- `total`：发现的候选文件总数；
- `planned`：成功生成候选名称的数量；
- `duplicates`：内容完全相同的重复文件数量；
- `errors`：无法读取或暂不支持的数量。

`planned` 是统计字段，表示成功生成了多少条建议；它不代表行状态是 `pending`。当前正常建议行的默认 `status` 仍然是 `approved`。

此时原文件名**没有发生变化**。

### 第 5 步：找到并打开 TXT 方案

方案位于：

```text
D:\DataRecovery\reports\01_rules_test_plan.txt
```

可以用 Excel、WPS 表格或文本编辑器打开。`.txt` 实际是带表头的制表符表格，保存时保留为文本格式即可，不要另存为 `.xlsx`。程序能自动读取 UTF-8、带 BOM 的 Excel Unicode 文本（UTF-16）、GB18030 和 Big5，并识别制表符、逗号或分号。

在 Excel/WPS 保存时：

1. 文件名保持 `.txt`；
2. 如果提示“此格式可能丢失功能”，选择“仍使用当前格式”“是”或同义选项；
3. 如果必须另存，选择“Unicode 文本（制表符分隔）”或 UTF-8 文本，并覆盖原来的 `.txt`；
4. 不要保存成 `.xlsx`，`apply` 不读取工作簿文件。

旧版本生成的“扩展名是 `.txt`、内容仍用逗号分隔”的方案也能直接读取，不需要手工转换。

方案中的字段如下：

| 列名 | 含义 | 是否需要你修改 |
|---|---|---|
| `status` | 当前处理状态 | **需要审核**。正常建议默认 `approved`；不执行时改为 `pending` |
| `original_relpath` | 相对于测试目录的原始路径 | 不要修改 |
| `original_name` | 当前文件名 | 不要修改 |
| `proposed_name` | 程序建议的新名称 | 可以修改，但必须保留扩展名 |
| `confidence` | 模型/规则给出的 0～100 置信度 | 仅供参考 |
| `category` | 推断的文档类型 | 可查看，不影响改名 |
| `reason` | 为什么建议这个名称 | 可查看，不影响改名 |
| `sha256` | 文件内容指纹 | 不要修改 |
| `duplicate_of` | 与哪个文件内容完全相同 | 不要修改 |
| `embedded_title` | 文档内部保存的标题 | 可查看 |
| `extraction_method` | 使用的提取方式 | 可查看 |
| `extraction_error` | 内容提取失败原因 | 可查看 |
| `size` | 文件大小，单位为字节 | 不要修改 |
| `modified_at` | 文件修改时间 | 不要修改 |

`modified_at` 使用 UTC 时间记录，因此可能与 Windows 资源管理器显示的本地时间相差几个小时；这不影响改名。

安全提示：如果原文件名、内部标题或模型理由的首个非空白字符为 `= + - @`，程序会在表格中加一个前导单引号，防止 Excel/WPS 将其作为公式执行；原值本来以单引号开头时会临时加倍，以保证可逆。日常只编辑 `status` 和 `proposed_name`，不要修改其他列；`apply` 和 `undo` 会自动还原真实值。

`status` 常见值：

| 状态 | 含义 | 建议处理 |
|---|---|---|
| `pending` | 暂不执行 | 用于不认可、尚未确认或准备以后处理的行 |
| `approved` | 已批准执行；正常建议的默认值 | `apply` 时会处理，所以运行前必须审核 |
| `duplicate` | 与另一个文件内容完全相同 | 暂时不要改，先人工判断是否保留 |
| `error` | 无法提取内容或 API 调用失败 | 查看 `extraction_error` |

### 第 6 步：批准一两个测试文件

先只选择 1～2 个不重要的测试文件：

1. 检查 `original_name`；
2. 检查 `proposed_name`；
3. 如果建议不理想，可以手动修改 `proposed_name`；
4. **不要删除 `.docx`、`.xlsx`、`.pdf` 等扩展名**；
5. 只保留 1～2 行为英文小写 `approved`；
6. 其他正常行先改成 `pending`，避免测试时一次改太多；
7. 保存为原来的 TXT 文件。

示例：

```text
准备执行：approved[TAB]20240101_001.docx[TAB]...[TAB]XX项目验收报告.docx
暂不执行：pending[TAB]20240101_002.docx[TAB]...[TAB]待进一步确认.docx
```

不要修改 `original_relpath` 和 `sha256`，否则安全校验可能失败。

### 第 7 步：执行已批准的改名

关闭正在占用方案文件或测试文档的程序，然后运行：

```powershell
.\.venv\Scripts\doc-renamer.exe apply ".\data\test" ".\reports\01_rules_test_plan.txt" --log ".\reports\01_rules_test_log.txt"
```

程序只会处理 `status=approved` 的行。其他行会跳过。

正常结束时会显示类似：

```json
{
  "approved": 2,
  "renamed": 2,
  "skipped": 8,
  "failed": 0
}
```

必须检查 `failed` 是否为 `0`。程序允许某一行失败后继续处理后面的行，因此即使命令执行结束，也要查看计数和日志中的 `message`。

现在回到：

```text
D:\DataRecovery\data\test
```

查看那 1～2 个文件是否已按预期改名。

改名日志保存在：

```text
D:\DataRecovery\reports\01_rules_test_log.txt
```

不要删除这个日志；撤销需要它。

### 第 8 步：测试撤销

运行：

```powershell
.\.venv\Scripts\doc-renamer.exe undo ".\data\test" ".\reports\01_rules_test_log.txt"
```

成功时，刚才改名的文件会恢复成原来的名称。

如果改名后又修改了文件内容，哈希会变化，程序会拒绝自动撤销，以免改错文件。

到这里，不联网的完整流程就测试完成了。

---

## 5. 第二阶段：配置和测试模型 API

只有在上一节全部正常后，再进行 API 测试。

### 5.1 先理解四个配置词

| 配置项 | 通俗解释 | 示例形式 |
|---|---|---|
| `provider` | 使用哪一种接口格式 | `responses` 或 `chat` |
| `api_base` | 服务商 API 的基础地址 | 以服务商文档为准；不填写具体接口路径 |
| `api_key` | 服务商给你的访问密钥 | 一串需要保密的字符 |
| `model` | 服务商提供的模型 ID | 必须使用账户实际可用的 ID |

本项目当前支持两种 API 接口：

1. `responses`：OpenAI Responses API；
2. `chat`：OpenAI-compatible Chat Completions，许多云端服务和本地服务采用这种格式。

OpenAI Responses API 的请求格式可参考[官方 OpenAI 文档](https://developers.openai.com/api/reference/cli/resources/responses/methods/create)。

### 5.2 创建真正生效的 `.env`

`.env.example` 只是示例，不会作为你的真实配置使用。

在 PowerShell 中确认当前位置是 `D:\DataRecovery`。下面的命令只会在 `.env` 不存在时复制模板，不会覆盖你已经填写的密钥：

```powershell
if (-not (Test-Path ".env")) { Copy-Item ".env.example" ".env" }
notepad ".env"
```

如果 `.env` 已经存在，上面第一行会自动跳过，第二行会直接打开现有文件；也可以单独运行：

```powershell
notepad ".env"
```

`.env` 的完整路径是：

```text
D:\DataRecovery\.env
```

配置 API 时，`.env` 中只能保留一个生效的 `DOC_RENAMER_PROVIDER`。模板最上方默认是 `rules`；请把这一行直接替换成 `responses` 或 `chat`，不要同时保留多个未注释的 provider。

请不要把 API 密钥写进：

- `.env.example`；
- Python 源代码；
- PowerShell 命令；
- TXT/CSV 方案；
- 发给其他人的截图或聊天消息。

### 5.3 配置 OpenAI Responses 接口

如果使用 OpenAI Responses 格式，把 `.env` 改成类似下面这样：

```dotenv
DOC_RENAMER_PROVIDER=responses
DOC_RENAMER_API_BASE=https://api.openai.com/v1
DOC_RENAMER_API_KEY=在这里填写你的真实API密钥
DOC_RENAMER_MODEL=在这里填写你的账户可用模型ID
DOC_RENAMER_TIMEOUT=90
DOC_RENAMER_RETRIES=3
```

注意：

- 基础地址填写到 `/v1` 即可，不要再追加 `/responses`；
- 模型 ID 应从你的 API 服务商控制台复制，不要凭空猜测；
- `DOC_RENAMER_API_KEY=` 后面不要保留“请填入密钥”等示例文字；
- 不要在行首加 `#`，带 `#` 的行是注释，不会生效。

### 5.4 配置 OpenAI-compatible Chat 接口

如果服务商说明它兼容 Chat Completions，则使用：

```dotenv
DOC_RENAMER_PROVIDER=chat
DOC_RENAMER_API_BASE=https://你的服务商基础地址/v1
DOC_RENAMER_API_KEY=在这里填写你的真实API密钥
DOC_RENAMER_MODEL=在这里填写服务商提供的模型ID
DOC_RENAMER_TIMEOUT=90
DOC_RENAMER_RETRIES=3
```

不同服务商的地址和模型名称不同，应以服务商自己的文档和控制台为准。

DeepSeek 当前已验证的配置示例：

```dotenv
DOC_RENAMER_PROVIDER=chat
DOC_RENAMER_API_BASE=https://api.deepseek.com
DOC_RENAMER_API_KEY=在这里填写你的真实API密钥
DOC_RENAMER_MODEL=deepseek-v4-flash
DOC_RENAMER_TIMEOUT=90
DOC_RENAMER_RETRIES=3
```

基础地址不要再追加 `/chat/completions`；程序会自动追加。旧的 `deepseek-chat/deepseek-reasoner` 已于 2026-07-24 停用；本项目默认示例使用速度较快的 `deepseek-v4-flash`，需要更强模型时可按账户权限改为 `deepseek-v4-pro`。模型名称后续可能变化，请以 [DeepSeek 官方更新日志](https://api-docs.deepseek.com/updates/) 为准。

### 5.5 只测试 API 连通性

保存并关闭 `.env` 后运行：

```powershell
.\.venv\Scripts\doc-renamer.exe probe-api
```

这个命令：

- `chat` 模式（包括 DeepSeek）正常情况下调用一次真实 API；如果返回空内容或错误 JSON，会自动再调用一次；网络失败还会按照 `DOC_RENAMER_RETRIES` 重试；`responses` 模式目前没有内容级重试；
- 使用程序内置的虚拟“项目周报”文字；
- 不读取 `data\test`；
- 不发送你的真实文档；
- 不修改任何文件。

成功时会看到包含下面字段的 JSON：

```json
{
  "title": "测试项目周报",
  "category": "周报",
  "confidence": 90,
  "reason": "文档内容说明了本周工作和下周计划"
}
```

文字不必完全相同，只要返回了合理的 `title`、`category`、`confidence` 和 `reason`，就说明接口基本可用。

### 5.6 用 API 处理少量测试文件

第一次只处理 5 个：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\test" --limit 5 --output ".\reports\02_api_test_plan.txt"
```

因为没有写 `--provider rules`，程序会读取 `.env` 中的 API 配置。

打开：

```text
D:\DataRecovery\reports\02_api_test_plan.txt
```

重点检查：

- 建议名称是否能准确概括内容；
- 合同、表格、报告等类别是否合理；
- 是否出现过于笼统的名称；
- 是否有多个文件被建议成相同名称；
- `extraction_error` 是否有错误信息；
- 敏感文件是否符合公司的数据处理要求。

这一阶段先看结果，暂时不一定要执行 `apply`。

---

## 6. 使用云端 API 时，什么内容会离开电脑

这是非常重要的安全问题。

API 模式下，程序可能发送以下内容：

- 当前恢复文件名；
- 文件类型；
- 文件修改时间；
- 文档内部标题；
- 提取出的正文节选；
- 内容提取错误信息。

默认限制是：

- 每个文档最多提取约 6000 个字符；
- PDF/PPTX 最多读取前 5 页或前 5 张幻灯片；
- 完全重复的文件不会重复调用模型；
- 无法提取文字的文件不会正常送去命名。

OpenAI Responses 请求中设置了 `store=false`，但这不等于适用于所有公司的保密要求。你仍需要确认：

- 公司是否允许文档内容发送到该服务商；
- 服务商的数据使用和日志策略；
- API 账户是否属于公司；
- 是否存在个人信息、合同、财务、人事或客户机密。

没有明确授权时，请继续使用 `rules`，或者等本地模型部署完成后再处理敏感文档。

---

## 7. 正式批量处理

只有满足下面条件后再进行：

- 不联网测试已经完成；
- API 虚拟内容测试成功；
- 少量真实副本测试结果可接受；
- 已确认数据合规要求；
- 原始恢复资料已另行保存；
- 已经测试过撤销功能。

### 第 1 步：准备完整工作副本

把完整恢复资料的**副本**放到：

```text
D:\DataRecovery\data\work
```

不要把 `data\test` 中已经改过的测试文件混入正式资料。

### 第 2 步：先生成较小批次

先处理前 100 个：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\work" --limit 100 --output ".\reports\03_work_batch_001_plan.txt"
```

检查正确率后，再决定是否扩大批量。

### 第 3 步：审核方案

建议按以下优先级检查：

1. 合同、财务、人事和客户文件逐份审核；
2. 低置信度文件逐份审核；
3. 名称相同或相似的文件逐份审核；
4. `duplicate` 文件单独判断；
5. `error` 文件查看错误原因；
6. 普通高置信度文件也要抽查。

不要根据 `confidence` 自动全选。置信度是模型自己的估计，不是真实准确率。

### 第 4 步：执行批准项

```powershell
.\.venv\Scripts\doc-renamer.exe apply ".\data\work" ".\reports\03_work_batch_001_plan.txt" --log ".\reports\03_work_batch_001_log.txt"
```

每个批次都使用不同的方案名和日志名，例如：

```text
03_work_batch_001_plan.txt
03_work_batch_001_log.txt
04_work_batch_002_plan.txt
04_work_batch_002_log.txt
```

程序默认拒绝覆盖已经存在的方案或日志。这是为了防止把以前的审核结果和撤销记录覆盖掉。

如果确实要覆盖，可以使用 `--overwrite`，但新手阶段不建议使用。更安全的做法是换一个新文件名。

### 第 5 步：保存结果

处理结束后，至少保存：

- 原始恢复资料；
- 改名后的工作副本；
- 每一批的 plan TXT；
- 每一批的 log TXT；
- 使用日期、接口、模型 ID 和操作者记录。

`reports` 中的方案和日志包含文件名、内部标题、哈希和错误信息，也可能属于敏感资料，请妥善保管。

---

## 8. 支持哪些文件

### 8.1 当前可以提取内容

| 类型 | 扩展名 | 提取方式 |
|---|---|---|
| Word 新格式 | `.docx`、`.docm` | 内部标题、正文、页眉页脚 |
| Excel 新格式 | `.xlsx`、`.xlsm` | 工作表名称、部分单元格内容 |
| PowerPoint 新格式 | `.pptx`、`.pptm` | 内部标题、前几页文字 |
| 文本 PDF | `.pdf` | 元数据标题、前几页可选择文字 |
| 普通文本 | `.txt`、`.md`、`.csv`、`.tsv`、`.json`、`.xml`、`.html`、`.htm`、`.log`、`.rtf` | 开头部分文字 |

程序只读取 Office 文件内部已经保存的内容，不会运行宏。Excel 公式也不会重新计算，只能读取文件中已经保存的值。

### 8.2 当前会标记为错误或待处理

| 类型 | 扩展名/情况 | 建议 |
|---|---|---|
| Word 旧格式 | `.doc` | 用可信的 Office/LibreOffice 另存为 `.docx` 副本 |
| Excel 旧格式 | `.xls` | 另存为 `.xlsx` 副本 |
| PowerPoint 旧格式 | `.ppt` | 另存为 `.pptx` 副本 |
| 图片 | `.jpg`、`.jpeg`、`.png`、`.bmp`、`.tif`、`.tiff` | 等后续接入 OCR |
| 扫描 PDF | PDF 内没有可选择文字 | 等后续接入 OCR |
| 加密文档 | 需要密码 | 在副本上解密后再试 |
| 损坏文件 | 无法正常打开 | 交给数据恢复人员进一步评估 |

“另存为”也应对副本操作，不要修改唯一原件。

其他未列出的格式，例如 `.gif`、`.webp`、`.heic`、`.odt`、`.xlsb`，当前会被直接忽略，通常不会出现在方案中。如果 `total=0`，也要检查测试目录里是否只有这些未支持格式。

---

## 9. 四个命令的通俗解释

### 9.1 `plan`：生成方案

```powershell
.\.venv\Scripts\doc-renamer.exe plan "输入目录" --output "方案.txt"
```

- 读取文件；
- 生成建议；
- 写入 TXT 制表符表格；
- 不改原文件名。

如果省略 `--output`，默认在当前 PowerShell 目录生成 `rename_plan.txt`。为避免找不到结果，本指南建议始终显式指定 `--output ".\reports\..."`。

常用选项：

| 选项 | 作用 |
|---|---|
| `--provider rules` | 强制不联网使用规则模式 |
| `--limit 20` | 最多处理 20 个文件 |
| `--max-chars 6000` | 正文节选最多提取的字符数；API 模式会连同少量元数据发送 |
| `--max-pages 5` | PDF/PPTX 最多读取的页数 |
| `--output 路径` | 指定方案 TXT/CSV 的保存位置 |
| `--overwrite` | 覆盖已有方案；新手不建议使用 |

### 9.2 `probe-api`：测试 API

```powershell
.\.venv\Scripts\doc-renamer.exe probe-api
```

- 读取 `.env`；
- 发送一份虚拟测试内容；`chat` 模式遇到空内容或错误 JSON 时可能重试；
- 不读取真实文件；
- 不改文件。

### 9.3 `apply`：执行批准项

```powershell
.\.venv\Scripts\doc-renamer.exe apply "输入目录" "方案.txt" --log "日志.txt"
```

- 只读取 `status=approved` 的行；
- 再次检查哈希；
- 检查扩展名和目标冲突；
- 在原目录内改名；
- 生成撤销日志。

如果省略 `--log`，默认在当前 PowerShell 目录生成 `rename_log.txt`。本指南建议每个批次都显式指定不同的日志路径。

### 9.4 `undo`：撤销

```powershell
.\.venv\Scripts\doc-renamer.exe undo "输入目录" "日志.txt"
```

- 从后往前读取日志；
- 检查文件是否仍是同一内容；
- 恢复改名前的名称；
- 有冲突或哈希变化时不恢复，并计入 `failed`。

---

## 10. 常见问题和排查

### 10.1 提示找不到 `doc-renamer.exe`

先确认文件是否存在：

```powershell
Test-Path "D:\DataRecovery\.venv\Scripts\doc-renamer.exe"
```

如果返回 `False`，在 `D:\DataRecovery` 中依次运行：

```powershell
py -m venv .venv
.\.venv\Scripts\python.exe -m pip install -e .
```

然后重新运行帮助命令。

### 10.2 提示找不到 `py`

说明 Python 没有安装或没有加入 PATH。请安装 Python 3.10 或更高版本，然后重新打开 PowerShell。

验证命令：

```powershell
py --version
```

### 10.3 提示“缺少 DOC_RENAMER_API_KEY”

检查：

1. 是否存在 `D:\DataRecovery\.env`；
2. 当前 PowerShell 是否位于 `D:\DataRecovery`；
3. 密钥行前面是否错误地加了 `#`；
4. `DOC_RENAMER_API_KEY=` 后面是否确实填写了内容；
5. 是否保存并关闭了记事本。

### 10.4 API 返回 401 或 403

通常表示：

- 密钥错误、过期或没有权限；
- 模型不属于当前账户；
- 服务商要求额外权限；
- API 地址和密钥不是同一家服务商。

请回到服务商控制台核对，不要把密钥发给他人排查。

### 10.5 API 返回 404

通常是基础地址或接口类型错误。

例如 Responses 模式的基础地址通常填写到：

```text
https://api.openai.com/v1
```

不要填写完整的 `/responses` 路径，因为程序会自己追加。

如果服务商只兼容 Chat Completions，应把 `DOC_RENAMER_PROVIDER` 改为 `chat`。

### 10.6 API 返回 429

通常表示请求频率、额度或余额受限。程序会自动尝试几次；仍然失败时，请检查服务商账户额度，或缩小批次后稍后重试。

### 10.7 `probe-api` 提示模型内容为空或标题字段无效

旧版本可能显示“模型返回的 title 为空”；当前版本可能显示“模型返回内容为空”“缺少可用标题字段”或“连续两次返回无效结果”。这些都不表示 DeepSeek 没有 Chat Completions 接口。模型有时会返回空内容，或把标题字段写成 `suggested_filename`。当前版本已经：

- 使用 JSON Output 模式；
- 明确要求 `title/category/confidence/reason` 四个字段；
- 兼容 `suggested_filename`；
- 对空内容或错误 JSON 自动重试一次。

请确认 `.env` 使用本指南中的 DeepSeek 配置，并重新运行：

```powershell
.\.venv\Scripts\python.exe -m pip install -e .
.\.venv\Scripts\doc-renamer.exe probe-api
```

### 10.8 提示 `UnicodeDecodeError`

这通常是 Excel/WPS 把方案保存成了 GBK/GB18030、Big5 或 UTF-16，而旧版本只按 UTF-8 读取。当前版本会自动识别 UTF-8、带 BOM 的 UTF-16、GB18030、Big5，也会自动识别制表符、逗号和分号。升级当前代码后，可以直接重试原来的 `.txt` 或 `.csv` 方案。

如果仍然报错，请确认文件没有被保存成 `.xlsx`、表头没有被删除，并尝试另存为“Unicode 文本（制表符分隔）”。程序会先完整解析和检查方案，再执行任何改名，因此格式错误不会造成半批次改名。

### 10.9 方案中全部是 `error`

查看 `extraction_error` 列。常见原因：

- 文件是旧版 Office 格式；
- PDF 是扫描件；
- 文件损坏；
- 文件正在被其他程序锁定；
- API 配置错误；
- 网络无法连接到服务商。

### 10.10 `status` 已经是 approved，为什么没有改名

检查：

- 必须是英文小写 `approved`；
- 方案文件是否已经保存；
- 是否执行了 `apply`，而不是再次执行 `plan`；
- `apply` 使用的是否正是刚才编辑的方案文件；
- 查看日志中的 `message`；
- 原文件是否被移动、删除或修改；
- 新文件名是否已经存在。

### 10.11 提示“文件哈希与方案不一致”

说明从生成方案到执行改名之间，文件内容发生了变化。不要强行绕过。请为当前文件重新生成一份新方案。

### 10.12 提示“目标文件已存在”

同一文件夹内已经有这个名字。回到方案文件，给 `proposed_name` 增加项目名、日期或序号，保存后重新执行。

例如：

```text
会议纪要.docx
会议纪要_2024-01.docx
会议纪要_项目A.docx
```

### 10.13 提示“建议名称必须保留原文件扩展名”

你可能把 `.docx`、`.xlsx`、`.pdf` 等结尾删掉或改掉了。恢复原扩展名后再执行。

### 10.14 方案或日志文件已经存在

程序默认不覆盖旧文件。请更换文件名，例如把：

```text
02_api_test_plan.txt
```

改成：

```text
02_api_test_plan_v2.txt
```

除非明确知道后果，否则不要使用 `--overwrite`。

### 10.15 中文显示乱码

TXT 默认使用 UTF-8 BOM 和制表符，Excel/WPS 通常可以正常识别。即使保存成带 BOM 的 Excel Unicode 文本（UTF-16）、GB18030 或 Big5，程序也会自动识别；分隔符可以是制表符、逗号或分号。不要另存为 `.xlsx`。

### 10.16 程序中途停止了怎么办

`plan` 会逐行写入 TXT，所以文件中通常保留已经处理的部分。不要直接拿不完整方案执行大批量改名。建议：

1. 保留不完整 TXT 用于排查；
2. 解决网络或文件问题；
3. 使用新的输出文件名重新生成方案；
4. 从少量文件重新测试。

---

## 11. 时间和费用怎么估算

### 11.1 规则模式

规则模式没有 API 费用。速度主要取决于：

- 文件数量；
- 文件大小；
- 磁盘速度；
- PDF 页数；
- 文件是否损坏。

### 11.2 API 模式

当前版本通常会为每个“非重复且成功提取内容”的文件调用一次模型 API。总时间和费用取决于：

- 文件数量；
- 每份文件发送的文字量；
- 模型价格；
- 服务商速度和频率限制；
- 网络情况；
- 失败重试次数。

建议按这个顺序扩大规模：

```text
1 个虚拟 API 测试
    ↓
5 个测试文件
    ↓
20 个测试文件
    ↓
100 个工作副本
    ↓
根据准确率和费用决定后续批次
```

不要一开始就对几万份文件发起 API 请求。

---

## 12. 一页命令速查表

所有命令执行前先进入项目目录：

```powershell
Set-Location "D:\DataRecovery"
```

查看帮助：

```powershell
.\.venv\Scripts\doc-renamer.exe --help
```

不联网生成 20 个测试文件的方案：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\test" --provider rules --limit 20 --output ".\reports\01_rules_test_plan.txt"
```

创建 API 配置：

```powershell
if (-not (Test-Path ".env")) { Copy-Item ".env.example" ".env" }
notepad ".env"
```

测试 API，不读取真实文件：

```powershell
.\.venv\Scripts\doc-renamer.exe probe-api
```

使用 API 生成 5 个测试文件的方案：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\test" --limit 5 --output ".\reports\02_api_test_plan.txt"
```

执行已批准的测试改名：

```powershell
.\.venv\Scripts\doc-renamer.exe apply ".\data\test" ".\reports\02_api_test_plan.txt" --log ".\reports\02_api_test_log.txt"
```

撤销测试改名：

```powershell
.\.venv\Scripts\doc-renamer.exe undo ".\data\test" ".\reports\02_api_test_log.txt"
```

正式工作副本前 100 个文件生成方案：

```powershell
.\.venv\Scripts\doc-renamer.exe plan ".\data\work" --limit 100 --output ".\reports\03_work_batch_001_plan.txt"
```

---

## 13. 最安全的推荐操作顺序

如果仍然觉得内容很多，只按下面顺序做：

1. 保管好原始恢复资料，不动它。
2. 复制 5 个普通文件到 `D:\DataRecovery\data\test`。
3. 打开 PowerShell，进入 `D:\DataRecovery`。
4. 运行规则模式的 `plan` 命令。
5. 打开 `D:\DataRecovery\reports\01_rules_test_plan.txt`。
6. 只批准 1 个文件。
7. 运行 `apply`，检查文件名。
8. 运行 `undo`，确认能恢复。
9. 创建并配置 `.env`。
10. 运行 `probe-api`。
11. 使用 API 测试 5 个文件，只看建议，不急着改名。
12. 统计准确率，确认数据合规，再考虑扩大批次。

只要始终使用副本、先生成方案、逐行审核并保存日志，这个流程就是可检查、可停止和可撤销的。
