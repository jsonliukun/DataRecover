"""恢复文档批量命名工具的包级命名空间。

导入 ``doc_renamer`` 时只定义运行时版本号，不创建网络客户端、不读取 ``.env``，
也不扫描或修改文件；因此其他程序可以安全地导入子模块并复用工作流。

模块职责：

* ``cli``：命令行参数、输出和退出码；
* ``workflow``：扫描、生成方案、执行改名和撤销；
* ``extractors``：从受支持格式中抽取元数据与正文节选；
* ``providers``：规则命名及远程模型适配；
* ``types``：跨层传递的数据对象；
* ``utils``：哈希、文本/文件名规整和路径边界检查。

``__version__`` 是供运行时代码读取的版本。项目元数据目前还在 ``pyproject.toml``
中声明版本，发布时两处必须同步；CLI 尚未实现 ``--version`` 参数。真正会改变
用户文档名称的入口只有显式调用 workflow 的 ``apply_plan`` / ``undo_log``（或
对应命令），而普通 import 与 ``plan`` 不会改动被扫描文档。
"""

__version__ = "0.1.0"
